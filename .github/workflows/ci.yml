name: Simple CI Pipeline

on:
  push:
   branches: ["main"]
  pull_request:
   branches: ["main"]  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: run a simple command
        run: echo "Hallaleuh"  

      - name: step1
        run: echo "step 1 is running finely" 

      - name: step2
        run: echo "step 2 is running finely"

      - name: Build Docker Image 
        run: docker build -t demo_devops

